#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../typedoc/bin/typedoc" "$@"
else
  exec node  "$basedir/../../../typedoc/bin/typedoc" "$@"
fi
# cmd-shim-target=/root/projects/Lecture-Tracker/node_modules/.pnpm/typedoc-plugin-coverage@4.0.2_typedoc@0.28.17_typescript@5.9.3_/node_modules/typedoc/bin/typedoc
