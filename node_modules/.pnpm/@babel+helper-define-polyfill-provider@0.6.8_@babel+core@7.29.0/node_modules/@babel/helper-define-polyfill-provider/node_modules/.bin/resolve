#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../resolve/bin/resolve" "$@"
else
  exec node  "$basedir/../../../../resolve/bin/resolve" "$@"
fi
# cmd-shim-target=/root/projects/Lecture-Tracker/node_modules/.pnpm/@babel+helper-define-polyfill-provider@0.6.8_@babel+core@7.29.0/node_modules/resolve/bin/resolve
