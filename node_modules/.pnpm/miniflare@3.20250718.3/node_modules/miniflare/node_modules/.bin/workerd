#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

exec "$basedir/../../../workerd/bin/workerd"   "$@"
exit $?
# cmd-shim-target=/root/projects/Lecture-Tracker/node_modules/.pnpm/miniflare@3.20250718.3/node_modules/workerd/bin/workerd
