#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../regjsparser/bin/parser" "$@"
else
  exec node  "$basedir/../../../regjsparser/bin/parser" "$@"
fi
# cmd-shim-target=/root/projects/Lecture-Tracker/node_modules/.pnpm/regexpu-core@6.4.0/node_modules/regjsparser/bin/parser
